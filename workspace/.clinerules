# Cline 开发规范 - 完整版

---

## 一、项目结构规范

### 目录结构
```
src/
├── api/                    # API 层
│   ├── client.ts          # axios 封装
│   ├── endpoints/         # 跨模块复用接口
│   └── modules/           # 业务模块接口
├── components/
│   ├── ui/               # 通用UI组件（无业务逻辑）
│   ├── biz/              # 业务组件（跨页面复用）
│   └── pages/            # 页面组件（特定页面独用）
├── features/              # 功能模块（按业务域划分）
│   └── xxx/
│       ├── api.ts        # 模块级接口
│       ├── hooks/        # 模块级 hooks
│       └── types.ts      # 模块级类型
├── hooks/                 # 全局工具 hooks
├── stores/               # 全局状态
├── types/                # 全局类型
├── utils/                # 工具函数
└── pages/                # 页面入口
```

---

## 二、代码规范

### 2.1 API 调用
- 简单/单次调用 → 可直接在组件内
- 同一逻辑重复调用2次以上 → 提取到同目录 `api.ts`
- 跨组件/跨模块复用 → 提取到 `src/api/endpoints/`
- 所有 API 必须导出 + 中文注释

### 2.2 组件分类
| 类型 | 判定标准 | 存放位置 |
|------|----------|----------|
| ui/ | 无业务逻辑，纯展示 | components/ui/ |
| biz/ | 跨页面复用 | components/biz/ |
| pages/ | 特定页面独用 | components/pages/ |

### 2.3 TypeScript
- 私用类型就近，复用类型全局
- 禁止使用 `any`，用 `unknown` 代替
- 开启 `strict` 模式

### 2.4 第三方组件
- 必须封装：ProTable、ProForm
- 建议封装：Modal、Upload
- 直接使用：Button、Input、Select、Tag

### 2.5 Hooks 组织
- 工具类 → `src/hooks/`
- 业务类 → `features/xxx/hooks/`

### 2.6 状态管理
- 优先本地状态
- 服务端数据用 React Query/SWR
- 全局状态超3个消费者才用 Zustand/Redux

---

## 三、AI 代码质量规范

### 3.1 命名规范
```
【命名 - 必须遵守】
- 禁止 a/b/data/info/tmp/res/ret/obj 等模糊名字
- 布尔值必须 isXxx/hasXxx/canXxx/shouldXxx 前缀
- 函数名必须是 verbObject 形式：fetchUserList、validateToken
- 常量全大写下划线分隔：MAX_RETRY_COUNT
- 完成后自检所有命名，不合规立即修正
```

### 3.2 边界处理
```
【边界检查 - 必须处理】
1. 空值/未定义：
   - 所有外部输入必须判空
   - 链式调用前用 ?. 和 ?? 保护
   - 示例：user?.profile?.avatar ?? '/default.png'

2. 类型安全：
   - 显式声明接口，不用 any
   - 类型断言前必须加类型守卫

3. 异步错误：
   - 所有 async/await 必须包 try-catch
   - Promise 必须有 .catch()

4. 输出要求：
   生成代码时，同步输出边界处理清单
```

### 3.3 重复代码 + 硬编码
```
【自检阶段】
每次生成代码后，检查：
1. 是否有重复代码块（≥3行相同）？
2. 是否有魔法数字/字符串？
3. 是否有硬编码路径？

报告格式：
✅ 无问题 / ⚠️ 建议改进 / ❌ 必须修复
```

### 3.4 注释规范
```
【分级注释】
- 公共API：强制 JSDoc
  * @description：为什么存在
  * @param：约束条件
  * @returns：返回值语义
  * @throws：已知异常

- 复杂逻辑：只写"为什么"
  * 多层嵌套（if/else > 2）
  * 外部状态依赖
  * 非显而易见的选择

- 简单函数：不写
  * 代码自解释时跳过
```

### 3.5 测试生成
```
【测试 Prompt】
为以下代码生成测试：
- 框架：Jest/Vitest
- 覆盖：正常输入 + 边界条件 + 异常处理
- 命名：describe-[功能名]，it-[具体行为]
- 关键断言必须有业务含义
```

### 3.6 需求确认
```
【确认机制】
执行前先判断：
1. 不可逆操作（删除/发送/付费）？→ 必须确认
2. 多种实现方式？→ 简述方案，等选择
3. 超过3步或多文件？→ 输出执行计划

确认格式：
【计划】1. xxx 2. xxx
【确认】请回复"继续"或修改
```

---

## 四、错误处理规范

### 4.1 API 错误
- 统一在 axios interceptor 中处理
- 401 跳转登录
- 网络错误统一提示

### 4.2 Error Boundary
- 页面级必须配置
- 独立功能区块（图表/评论）按需配置

### 4.3 关键路径
- 写操作（下单/支付/提交）必须 try-catch + 用户提示

---

## 五、工具约束

### 5.1 ESLint 配置
```json
{
  "max-lines": 300,
  "complexity": 15,
  "no-unused-vars": "error",
  "no-console": "warn"
}
```

### 5.2 TypeScript 配置
```json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true
  }
}
```
